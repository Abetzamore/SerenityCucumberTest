serenity {
    take.screenshots = FOR_FAILURES
}

headless.mode = false

webdriver {
  driver = chrome
  autodownload = tru
}

chrome.switches = """--start-maximized;--test-type;--no-sandbox;--ignore-certificate-errors;
                    --disabled-popup-blocking;--disabled-default-apps;--disabled-extensions-file-access-check;
                    --incognito;--disabled-inforbars;--disabled-gpu""""